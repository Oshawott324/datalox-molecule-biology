{
  "schema": "datalox.molecule.eval-task",
  "version": "0.1.0",
  "id": "mb-edit-puc19-mcs-insert",
  "title": "pUC19 MCS NotI insert with bla CDS frame preserved",
  "category": "sequence_edit",
  "objective": "Import authentic pUC19, insert a NotI payload after EcoRI in the MCS, validate the workspace, and render the edited plasmid map.",
  "constraints": [
    "Use public MCP tools only.",
    "Do not patch molecule.workspace.json directly.",
    "Call validate_workspace with checkSequenceDigests=true after edit_sequence.",
    "Do not infer amino-acid consequences from edit_sequence alone."
  ],
  "inputs": [
    {
      "id": "puc19",
      "path": "inputs/puc19.gb",
      "kind": "genbank",
      "sha256": "9dd4994440098931e5efd8382573d1923795f736281ca0c231857585407068db",
      "source": "Repo fixture copied from fixtures/genbank/puc19.gb."
    }
  ],
  "toolPlan": [
    {
      "step": 1,
      "tool": "open_sequence",
      "purpose": "Import task input into a fresh workspace.",
      "required": true
    },
    {
      "step": 2,
      "tool": "edit_sequence",
      "purpose": "Apply the pinned sequence edit through revision-safe tool writes.",
      "required": true
    },
    {
      "step": 3,
      "tool": "validate_workspace",
      "purpose": "Verify workspace JSON and stored sequence digest consistency.",
      "required": true
    },
    {
      "step": 4,
      "tool": "get_sequence_context",
      "purpose": "Read the edited sequence region and features.",
      "required": true
    },
    {
      "step": 5,
      "tool": "find_restriction_sites",
      "purpose": "Verify the inserted NotI restriction site deterministically.",
      "required": true
    },
    {
      "step": 6,
      "tool": "render_plasmid_map",
      "purpose": "Render byte-stable plasmid map artifact.",
      "required": true
    }
  ],
  "expected": {
    "summaryPath": "expected/summary.json",
    "toolObservationsPath": "expected/tool-observations.json",
    "artifactsPath": "expected/artifacts.json"
  },
  "grading": {
    "mode": "exact_json_subset",
    "artifactHashRequired": true
  },
  "caveats": [
    "The authentic fixture annotates lacZalpha as a non-CDS gene feature, so this task only asserts bla CDS frame preservation."
  ]
}
